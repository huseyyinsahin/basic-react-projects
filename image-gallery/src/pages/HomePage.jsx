import Image from "../components/Image";
import data from "../helper/data.js";

const HomePage = () => {
  return (
    <div>
      <Image data={data} />
    </div>
  );
};

export default HomePage;
